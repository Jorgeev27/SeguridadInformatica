# This Python code snippet is calculating the perimeter of a circle based on the input radius provided
# by the user. Here's a breakdown of what each line does:

import math;
radio = float(input("Dime el radio: "));
print("El perímetro es:", (math.pi*radio**2, 2*math.pi*radio));