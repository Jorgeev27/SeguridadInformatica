# This Python code prompts the user to enter a word using the `input()` function. The entered word is
# stored in the variable `palabra`.

palabra = input("Dime una palabra: ");
print((palabra + " ") * 1000);