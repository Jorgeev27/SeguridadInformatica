# This Python code snippet is prompting the user to enter their name by displaying the message "¿Como
# te llamas?". The user's input is then stored in the variable `nombre`. 
# After that, the code prints a greeting message "Hola" followed by the value
# stored in the `nombre` variable, which is the name entered by the user.

nombre = input("¿Como te llamas? ");
print("Hola", nombre);